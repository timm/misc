#!/usr/bin/env ./gold
# vim: filetype=awk ts=2 sw=2 sts=2  et :

@include "rows"

function _rows(    i,d,r) {
  d=AU.dot
  Rows(i)
  RowsRead(i, d d "/data/weather" d "csv")
  oo(i.cols[1])
  #RowsDivs(i)
  #for(r in i.rows) o(i.rows[r].ranges)
}
BEGIN { rogues(); _rows() }
