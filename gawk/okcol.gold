#!/usr/bin/env ./gold
# vim: filetype=awk ts=2 sw=2 sts=2  et :

@include "col"
@include "num"
@include "sym"
@include "shared"

function _all(    a,how,j) {
  print 22
  split("a a a a b b c",a," ")
  all(a,how)
  oo(how)
  print Var(how)
} 

BEGIN { _all() }
