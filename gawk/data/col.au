# /* vim: set nospell filetype=awk ts=2 sw=2 sts=2 expandtab: */
@include "au"


function Col(i,pos,txt) {
   Object(i)
   i.txt = txt
   i.pos = pos="" ? 1 : pos
}

function ColPos(i) { return i.pos }
